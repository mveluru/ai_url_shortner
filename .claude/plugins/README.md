# Plugins — cross-component workflows
Use alongside the generic `engineering:*` skills (code-review, testing-strategy, incident-response, deploy-checklist); these add project-specific procedure.

`new-endpoint` · `new-failure-mode` · `contract-change` · `security-review` · `incident-response` · `release-checklist`
