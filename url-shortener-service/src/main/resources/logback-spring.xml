<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging (design doc sections 12.2, 20.5).
  - Local/test: a readable console line that always shows the requestId.
  - staging/prod: structured JSON, so requestId / traceId / error code are queryable fields rather than grep patterns.
  requestId is placed in the MDC by RequestIdFilter for the whole request, so every line carries it, including lines
  emitted before an exception is thrown. Never logged anywhere: raw IPs, API keys, query strings, long URLs, headers.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="prod | staging">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <!-- A stack trace stays in the logs (server side) and is never in an HTTP response body. -->
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerThrowable>30</maxDepthPerThrowable>
                    <rootCauseFirst>true</rootCauseFirst>
                </throwableConverter>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

    <springProfile name="!prod &amp; !staging">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%X{requestId:--}] %logger{28} - %msg%n%wEx</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
